public class Circle {
    private double PI;
    public void name(double PI){
        double radius = PI;
        double name = Math.PI*(radius*radius);
        System.out.println("площадь окружнение:"+name );
        double circumference = Math.PI*2*radius;
        System.out.println("длина окружности равно:"+circumference);
    }
}
